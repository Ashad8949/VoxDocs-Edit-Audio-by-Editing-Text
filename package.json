{
  "name": "voxdocs",
  "version": "1.0.0",
  "private": true,
  "description": "Edit audio and video by editing their transcript",
  "type": "module",
  "workspaces": ["packages/*", "services/api", "web"],
  "scripts": {
    "test": "npm run test --workspaces --if-present",
    "dev:api": "npm run dev --workspace services/api",
    "dev:web": "npm run dev --workspace web",
    "build:web": "npm run build --workspace web"
  },
  "license": "MIT"
}
