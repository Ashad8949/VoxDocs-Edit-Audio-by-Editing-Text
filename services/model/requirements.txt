flask>=3.0,<4
gunicorn>=21.2
numpy>=1.24
faster-whisper>=1.0

# Translation (IndicTrans2 en<->hi)
torch
transformers==4.57.6
sentencepiece
indic-nlp-library
sacremoses

# Voice-preserving synthesis (XTTS-v2 cross-lingual cloning)
coqui-tts
torchaudio
torchcodec

# Pro-tier RVC inference. Its fairseq dep needs Python <=3.11 (the container is
# python:3.11-slim); on a newer local interpreter it won't install and the Pro
# tier transparently falls through to XTTS. soundfile is used to pipe audio in/out.
rvc-python; python_version < "3.12"
soundfile
